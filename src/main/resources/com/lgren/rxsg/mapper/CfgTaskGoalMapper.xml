<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lgren.rxsg.mapper.CfgTaskGoalMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lgren.rxsg.entity.CfgTaskGoal">
        <id column="id" property="id" />
        <result column="tid" property="tid" />
        <result column="content" property="content" />
        <result column="sort" property="sort" />
        <result column="type" property="type" />
        <result column="count" property="count" />
        <result column="reduce" property="reduce" />
        <result column="strong_level" property="strongLevel" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tid, content, sort, type, count, reduce, strong_level
    </sql>

</mapper>
